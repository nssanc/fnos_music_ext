# 更新记录

本文记录 `fnmusic-ext` 已合入主分支的主要用户可见改动。更细的实现差异以 Git 提交历史为准。

## 2026-09-07

### 合并上游 v1.1.2

- 新增独立的洛雪聚合服务，可直接参与酷狗、网易云、咪咕搜索，并提供歌词、封面和多档音质解析；与原有洛雪自定义脚本运行器并存，分别使用 `8773` 和 `8772`。
- 多音源搜索改为约 3 秒首屏预算、无结果时最多再等待 5 秒，后台继续聚合；搜索缓存默认延长到 7 天。
- 加入试听、收费、明确不可播以及无效直链的多层过滤，并保留现有的 30 秒流大小校验和完整歌曲自动换源。
- 增加 `VERSION`、安装配置增量合并、重复安装保护、LLM 模型列表读取以及更稳健的安装/还原检查。

### 第三方客户端与播放完整性

- 支持使用飞牛原生 `/music/api/v1` 与 `music-token` 的第三方客户端搜索、查看元数据/封面/歌词及在线播放在线曲目。
- 修复第三方客户端的在线歌曲、歌手、专辑封面：实体返回非空 `coverId`，NAS 直接代理图片内容，不再依赖客户端跟随外部 CDN 的 302 跳转。
- 实际在线流 `GET` 会补记播放历史；同时兼容更多 `/event/report` 字段格式，最近播放记录包含完整歌曲、歌手、专辑和封面数据。
- 流接口兼容 `GET`、`HEAD` 和 `Range`；媒体大小未知时不再错误返回 `Content-Length: 0`。
- 对数据库中仍存在、但磁盘文件已经删除的本地歌曲，在搜索、曲库、收藏、歌单、歌手/专辑及最近播放列表统一过滤，并修正列表总数。
- 识别并拒绝与原曲时长明显不符的 30 秒试听流；自动在网易云、QQ 音乐和 musicdl 中重新匹配并优先选择可验证的完整歌曲。

### 移动端 Web 与播放页

- 首页、搜索、歌曲列表和导航取消固定桌面宽度，适配手机窄屏与底部安全区域。
- 全屏播放页改为移动端单列布局，修复封面、歌曲信息、控制条和歌词互相挤压或横向溢出的问题。
- 歌词区域获得独立可滚动视口，并与底部导航和播放器控制区保持间距。

### 音源、歌词源与媒体补全

- 在歌曲和播放页显示当前音乐来源，并支持手动切换播放音源。
- 将已导入的洛雪自定义源接入播放页音源菜单和“优先歌曲源”：可用当前平台歌曲 ID 强制解析洛雪播放地址，本地缓存歌曲会先按歌名和歌手恢复平台 ID。
- 修复移动端点击「音源」无反应：提高菜单层级与触控优先级，扩展 Fetch/XHR/HLS 当前歌曲识别，并支持已被飞牛重新扫描为本地 GUID 的缓存歌曲换源。
- 修复切换音源时误报“未找到播放器”：入口脚本改为在飞牛主模块前加载，并跟踪飞牛通过 `new Audio()` 创建、未挂载到页面 DOM 的播放器；同时兼容 `video` 和开放 Shadow DOM 中的媒体元素。
- 音频源与歌词源可独立选择；自动模式按标题、歌手、时长和完整性选择更合适的来源。
- QQ QRC/XML、Base64 和 HTML 实体歌词会转换为标准 LRC 时间轴，支持逐行高亮与自动滚动；其他来源沿用同一标准化流程。
- 严格校验歌词缓存的歌曲名和歌手，避免相似文件误匹配无关歌词。
- 搜索和每日推荐缺少封面时，可从 QQ 音乐、网易云等已启用来源交叉补全。

### 搜索、歌手页与来源管理

- 搜索支持后台聚合和连续翻页，不再固定只显示前 30 首；网易云和 musicdl 默认最大抓取量提高至 100。
- 修复 QQ 音乐新版搜索响应解析及稳定页大小，改善 QQ 结果数量与可用性。
- 在线歌手/专辑采用稳定标识，补齐详情、歌曲、专辑和时间字段，修复从搜索结果点击歌手后页面加载失败。
- 设置中支持 QQ 音乐二维码登录、启停来源，以及添加/删除洛雪自定义源。

### 缓存目录与部署

- 完整歌曲按 `音乐库/歌手名/歌名.扩展名` 保存，歌词以同名 `.lrc` 放在歌曲旁边；同名歌手复用同一目录。
- 后台独立下载任务在客户端切歌或断开后仍可完成校验；不完整下载会清理 `.part`，不会作为正式音乐缓存保留。
- 修复 Docker daemon 权限相关部署流程和依赖构建缓存，更新安装、启用、还原与健康检查说明。

## 说明

- QQ 音乐音质取决于登录账号的实际权益；本项目不会绕过平台会员或版权限制。
- 删除文件后第三方客户端若仍显示旧项目，先刷新客户端曲库或清除其本地列表缓存；服务端会在响应中自动过滤磁盘已不存在的项目，但不会直接改写飞牛官方数据库。
- 通用 Subsonic/OpenSubsonic 协议不等同于飞牛原生 API，目前需另配协议桥接。
